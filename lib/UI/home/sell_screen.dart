import 'package:flutter/material.dart';

class SellNowScreen extends StatelessWidget {
  const SellNowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Sell Now"),
      ),
    );
  }
}
